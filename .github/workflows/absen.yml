name: Auto Absen UNBIN

on:
  schedule:
    - cron: '30 2 * * *'  # 09:30 WIB (02:30 UTC)
    - cron: '30 6 * * *'  # 13:30 WIB (06:30 UTC)
  workflow_dispatch:
  push:

jobs:
  absen:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.10'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install selenium webdriver-manager pytz
        
    - name: Run absen script
      env:
        UNBIN_USERNAME: ${{ secrets.UNBIN_USERNAME }}
        UNBIN_PASSWORD: ${{ secrets.UNBIN_PASSWORD }}
      run: python absen.py
      
    - name: Upload log files
      if: always()  # Selalu upload log bahkan jika script gagal
      uses: actions/upload-artifact@v2
      with:
        name: absensi-logs
        path: logs/
        retention-days: 7  # Simpan log selama 7 hari